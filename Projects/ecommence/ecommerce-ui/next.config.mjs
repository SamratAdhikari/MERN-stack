/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ["cdn.achology.com", "images.unsplash.com"],
    },
};

export default nextConfig;
